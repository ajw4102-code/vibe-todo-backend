# todo-backend

Minimal Node.js + Express scaffold for the todo backend.

Quick start

```bash
# install dependencies
npm install

# run in development (requires nodemon)
npm run dev

# run production
npm start
```

Server runs on port 3000 by default.
